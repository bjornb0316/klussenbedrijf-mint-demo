document.addEventListener('DOMContentLoaded',()=>{
  const toggle=document.querySelector('.nav-toggle'), menu=document.querySelector('.nav-mobile');
  if(toggle&&menu){toggle.addEventListener('click',()=>{const open=menu.classList.toggle('is-open');toggle.setAttribute('aria-expanded',open)});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('is-open')))}
  const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
  document.querySelectorAll('[data-wa]').forEach(a=>{const message=a.dataset.waMessage||'Hallo Luka, ik wil graag mijn klus bespreken.';a.href='https://wa.me/31617321266?text='+encodeURIComponent(message)});
  document.querySelectorAll('form[data-demo-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const success=form.parentElement.querySelector('.form-success');if(success){success.classList.add('is-visible');form.reset();success.scrollIntoView({behavior:'smooth',block:'nearest'})}}));
  const funnel=document.querySelector('[data-funnel]'); if(funnel){let step=0;const steps=[...funnel.querySelectorAll('.funnel-step')], progress=funnel.querySelector('.funnel-progress span'), back=funnel.querySelector('[data-back]'), next=funnel.querySelector('[data-next]'), chosen={}; const render=()=>{steps.forEach((s,i)=>s.classList.toggle('active',i===step));if(progress)progress.style.width=((step+1)/steps.length*100)+'%';if(back)back.hidden=step===0;if(next)next.textContent=step===steps.length-1?'Aanvraag versturen':'Verder'}; funnel.querySelectorAll('.choice').forEach(btn=>btn.addEventListener('click',()=>{btn.parentElement.querySelectorAll('.choice').forEach(x=>x.classList.remove('selected'));btn.classList.add('selected');chosen[btn.closest('.funnel-step').dataset.key]=btn.dataset.value;if(step<steps.length-1){step++;render()}})); next?.addEventListener('click',()=>{if(step<steps.length-1){step++;render();return} const done=funnel.querySelector('.funnel-done'); steps.forEach(s=>s.classList.remove('active'));if(done)done.classList.add('active');}); back?.addEventListener('click',()=>{if(step>0){step--;render()}});render()}
});
